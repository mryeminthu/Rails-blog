<%= form_with(model: [@post, @post.comments.build(user: @current_user)], url: post_comments_path(@post)) do |form| %>
    <div>
      <%= form.label :text %>
      <%= form.text_area :text %>
    </div>
    <div>
      <%= form.submit "Add Comment" %>
    </div>
  <% end %>
  